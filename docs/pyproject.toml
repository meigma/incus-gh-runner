[project]
name = "incus-gh-runner-docs"
version = "0.0.0"
description = "Documentation site for incus-gh-runner."
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.0",
]

[tool.uv]
package = false
