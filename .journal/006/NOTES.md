---
id: 006
title: Continue phase 4 GitHub lifecycle
started: 2026-07-17
---

## 2026-07-17 20:52 — Kickoff
Goal for the session: Continue the v1 implementation plan with phase 4, integrating the real GitHub scale-set lifecycle and proving one genuine queued job on one JIT-configured Incus runner VM.
Current state of the world: Phases 0 through 3 are merged on `master` at `d03cace`; the controller core, reference image and guest contract, and ownership-scoped Incus lifecycle are implemented, while GitHub demand and JIT composition remain unwired and the Incus-capable live gates have not yet been run.
Plan: Start with the smallest phase 4 proof, settle the development credential interface, add the real scale-set adapter and composition, then exercise the narrowest available end-to-end evidence at `min_runners: 0` and `max_runners: 1`.

## 2026-07-17 21:06 — Live test environment
The preferred live-test environment is a minimal Latitude.sh server provisioned through the already authenticated and billing-enabled local `lsh` CLI. Because provisioning and teardown take minutes and billed runtime should stay minimal, finish and rehearse the implementation, configuration, image transfer, validation commands, and cleanup locally before provisioning. Use one scripted hardware window to run the pending phase 2 image boot gate, phase 3 Incus lifecycle gate, and phase 4 genuine GitHub job proof, capture evidence, remove all test resources, and then destroy the server.

## 2026-07-17 21:50 — Phase 4 local slice and GitHub preflight
Created the isolated `feat/phase-4-github-lifecycle` Worktrunk worktree from `origin/master` at `7290aa5`. Commits `1374e2c` and `05b8f88` add persistent scale-set resolution, message polling, non-blocking demand publication, per-VM JIT generation, production Incus composition, environment-only PAT and GitHub App credential interfaces, protected diagnostics, an opt-in real GitHub preflight, the manual one-shot runner workflow, and pre-hardware live-test scripts. The real repository GitHub preflight passed with the existing `gh` credential by resolving `incus-gh-runner-phase4` and opening and closing a message session; `mise exec -- moon run root:check --summary minimal` also passed. Next: publish the branch, validate hosted CI, merge the workflow to the default branch, prepare the reference-image/binary bundle, and only then allocate the minimal Latitude host for the phase 2 through 4 acceptance window.

## 2026-07-17 23:10 — Hardware boot proof and Noble GRUB correction
Merged phase 4 as PR #11 (`e778ef1`) after local and hosted gates passed, then allocated the minimal Latitude.sh `c3-small-x86` host for one consolidated hardware window. Incus 6.0 on Ubuntu 24.04 exposed two reference-image compatibility gaps before the controller proof: the validator used an unsupported `incus image info --format json` invocation, and the image had no UEFI bootloader. PR #12 corrects the validator and installs signed UEFI GRUB. A disposable VM then isolated an Ubuntu Noble regression where `grub-install --removable` reaches a GRUB prompt despite a correct config; rerunning the same signed install without `--removable` booted successfully, the Incus agent became ready, and systemd reported `running`. Commit `6b012cc` carries the verified flag change and its local full gate passes. Next: consume the rebuilt PR artifact, run the controller plus one genuine GitHub Actions job, capture cleanup evidence, merge PR #12, and destroy the Latitude server.

## 2026-07-17 23:36 — Paused after hardware teardown
The final PR #12 reference-image artifact from run `29633456601` passed its checksum (`68449dadbf1bd68b31f0e9cdaa182ecdac338cf6dfa40ae81b0cc5125c9e01dd`). On hardware it booted, accepted the probe payload, reported `running`, removed the payload, executed the non-root probe, and powered off. Ubuntu's Incus 6.0.0 could not retrieve VM console logs; upgrading the disposable host from the fingerprint-verified Zabbly stable repository to Incus 7.2 enabled the VM console endpoint, but the stopped VM returned an empty buffered log, so the validator's final log assertions still need investigation. No GitHub credential was transferred and the controller/workflow proof was not started. The host had zero instances and zero images before teardown. Evidence is saved locally under `build/live-phase4-evidence/phase4-latitude-20260718.tar.gz` (SHA-256 `57f43afe48dc35078dcf4e646d1df0843c9ef2101744972ec7a7b114e33284b6`). Latitude server `sv_x3egaQZVQ046Q` was destroyed successfully and a follow-up lookup returned 404. Resume with PR #12 and decide whether the VM serial console must be drained while running or whether protected guest status is the better diagnostic contract, then allocate a fresh host only after that path is prepared locally.

## 2026-07-18 08:54 — Live diagnostic capture correction
The empty stopped-VM console was caused by the guest powering QEMU off before Incus could drain its ring buffer; Incus only persisted the buffer reliably when the host initiated shutdown. Commit `d07aa96` keeps runner output in the guest journal, writes only lifecycle metadata directly to `ttyS0`, publishes terminal status, holds a 30-second fallback grace window, and updates the validator to capture live serial history before verifying guest self-poweroff. The validator now requires Incus 6.5 or newer because VM console history landed after the Ubuntu 24.04 native 6.0 release. The full local gate passes, CI and Pages are green, and final Reference Image run `29650876512` is building. Do not allocate replacement hardware until that artifact succeeds and its checksum is verified locally.

## 2026-07-18 09:44 — Phase 4 hardware proof complete
Completed the consolidated hardware window on Latitude server `sv_XDO7NYvEP5Pgw` using Incus 7.2 and exact reference-image artifact `77f7614c453917873a45fda68926a5ca0eb849ad2b488b7aab5ff145fd60fe91`. The image validator passed the boot, agent, payload-consumption, non-root probe, live serial capture, self-poweroff, and cleanup contract. The phase 3 functional proof then exposed two production integration gaps: the Incus SDK's `GetImage` does not resolve aliases, and a guest can publish terminal status while still running during its diagnostic grace. Commits `4c4cbbb` and `7852df1` resolve configured aliases and explicitly stop an owned terminal VM before deletion; the full local gate and the hardware functional test then passed with zero remaining owned instances.

The real controller opened scale set `incus-gh-runner-phase4`, accepted one assigned job, created one JIT-configured VM, and ran GitHub Actions workflow `29652301896` / job `88100583500` successfully. Controller evidence confirms demand returned from one to zero and exact runner deletion. The protected console artifact was root-owned mode `0600`, contained `state=running` and `action=poweroff exit_code=0`, and did not contain the GitHub credential. Evidence is stored under `build/live-phase4-evidence/20260718-run-29652301896/`; the host archive checksum is `25bd222ab7a16e31a5fc1b403731e31d415b2dd87ce60705983227b5059ff3ec`. The runtime credential and systemd proof unit were removed before export. Latitude server `sv_XDO7NYvEP5Pgw` was destroyed and a follow-up lookup returned `404 NotFound`.

PR #12 merged to `master` as squash commit `8580997` after exact-head CI, Pages, Kusari, and Reference Image run `29652218136` passed. Local `master` was fast-forwarded and the merged feature worktree and branch were removed. Phase 4 implementation and acceptance evidence are complete; keep this journal session open until explicitly closed.
